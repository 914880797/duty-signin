# 管理员账号配置文件
# 使用说明：
# 1. 复制此文件为 admin-config.json（不带.example 后缀）
# 2. 修改 username 和 password 为你自己的账号密码
# 3. 运行初始化脚本将账号写入数据库
# 4. 删除 admin-config.json 文件（避免密码泄露）
#
# 注意：此文件包含敏感信息，不要提交到 Git！

{
  "admins": [
    {
      "username": "admin",
      "password": "your_password_here",
      "is_active": true
    }
  ]
}
